//ready es el metoddo que asigna una funcion el evento onload de la pagina
jQuery(document).ready(function()
    {
      window.alert("lista la pagina");
      this.body.innerHTML+="generando cpn jquery"
    }
)